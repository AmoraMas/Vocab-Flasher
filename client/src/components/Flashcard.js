import React from "react";
import PropTypes from "prop-types";

function Flashcard() {
  return (
    <div className="flashcard">
      <p>Test Page for Flashcard</p>
    </div>
  );
}

// Categories.propTypes = {
//   categories: PropTypes.array.isRequired,
// };

export default Flashcard;
